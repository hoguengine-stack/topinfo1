# TOPINFO 작업 인계

## [58차 작업] 공개 경로·임직원 권한·개인정보 고지 릴리스 게이트 - 2026-07-18

- 실제 운영 저장소 `C:\탑정보통신-작업-관리`에서 기존 Vite·React·Firebase·백오피스를 유지한 채 작업했습니다.
- 공개 표준 경로를 12개로 확장하고 각 경로의 정적 HTML 셸, 404 셸, sitemap, canonical, Open Graph URL을 일치시켰습니다. 미등록 경로는 브랜드 404와 `noindex,nofollow`를 사용합니다.
- 접속코드·해시·사용자 문서 플래그를 임직원 권한 근거에서 제거했습니다. Google 로그인 제공자와 `settings/security.adminUids`·`employeeUids` 허용 목록만 백오피스 권한을 부여합니다.
- 클라이언트의 `settings/security` 생성과 `access_verifications/*` 읽기·쓰기를 전면 차단했습니다. 건의글 생성 시 답글 배열과 타인 UID 주입도 Rules와 두 작성 경로에서 차단했습니다.
- 상담·용지·건의 작성 화면에서 개인정보처리방침 전문을 직접 열 수 있게 했습니다. 건의 작성 중 전문을 열고 닫아도 입력 상태가 보존됩니다.
- `npm test` 58/58, `npm run lint`, `npm run build`가 통과했습니다. 빌드는 공개 정적 셸 12개와 404 셸을 생성합니다.
- 전체 공개 QA는 12개 경로 x 11개 뷰포트 = 132건에서 P0/P1/P2 0입니다. 이번 개인정보 링크 변경 범위 4건과 모달 직접 동작 3건도 통과했습니다.
- 운영 `https://topinfo.co.kr/`는 아직 이전 번들 `index-CArg-aCd.js`를 제공합니다. `robots.txt`와 `sitemap.xml`도 홈 HTML을 반환합니다. 승인 전 push·Firebase Rules·Hosting 배포를 하지 않았기 때문입니다.
- 프로덕션 공개는 HOLD입니다. 파트너 이미지 권리, `settings/security` UID 사전 구성, 실제 계정 권한과 폼→접수함→작업 전환 왕복, App Check enforcement·스팸 방어가 남아 있습니다.
- 운영 권한 전환 순서: `docs/staff-access-migration-2026-07-18.md`
- 현재 릴리스 게이트: `output/audits/release-gate-current-2026-07-18.md`
- 종합 보고서: `docs/release-candidate-report-2026-07-18.md`

## Retrospective & Future Improvements (회고 및 개선 방향)

- 접속코드 해시를 Firestore Rules에서 비교하는 방식은 해시 자체가 재사용 가능한 자격 증명이 되므로 서버 검증이 아니다. 운영 권한은 관리자가 통제하는 UID allowlist나 서버 발급 custom claims로 관리해야 한다.
- 정적 Rules 테스트는 위험한 코드 패턴의 재도입을 빠르게 막는 용도다. 실제 `get()` 판정, 쿼리, 배치 쓰기, App Check는 Emulator와 운영 계정으로 별도 확인해야 한다.
- 보안 변경은 코드와 Rules보다 데이터 마이그레이션 순서가 먼저다. `adminUids`가 없는 상태에서 새 Rules를 배포하면 최고 관리자가 잠길 수 있으므로 배포 체크리스트를 차단 게이트로 유지한다.
- 브라우저 QA는 전체 132건을 매 수정마다 반복하지 않고 변경 라우트만 좁혀 확인했다. 공통 레이아웃·릴리스 경계가 바뀔 때만 전체 매트릭스를 다시 실행하는 것이 비용 대비 정확하다.
- 현재 시각 P2는 권리 확인된 고해상도 파트너 원본이 필요한 항목이다. 잘못된 생성 이미지나 무단 크롭으로 빠르게 감추는 작업은 하지 않는다.

## [59차 작업] 공개 미디어 재유입 차단·Rules Emulator - 2026-07-18

- 공개 부적합 자산 26개를 `public/`에서 `.asset-research/rights-pending/`로 이동했습니다. 이번 단계에서 24개를 `public-blocked/`로 옮겼고 앞서 분리한 2개도 같은 차단 게이트에 포함했습니다. 비공식 영수증·후기, 조건 미확인 쿠폰·고객 예시, 반려된 리테일 장면, 구형 U+ 이미지, 제품 합성, `generated/system-*`가 대상입니다.
- `src/utils/publicMedia.ts`를 단일 차단 목록으로 사용하고 CMS 게시 감사, 커스텀 페이지 복원, 업종 상세, 제품 정규화·저장, 공개 렌더러가 같은 기준을 적용하도록 했습니다.
- `scripts/rebuild-core-pos-assets.py`는 반려 자산을 다시 `public/`에 쓰지 않고 권리 검토 폴더로만 생성합니다. 차단 파일이 공개 폴더에 다시 생기면 `test:media`가 실패합니다.
- 관리자 PC 파일 선택은 실제 업로드가 아니라 임시 미리보기임을 명확히 표시하고 `internal_only` 상태를 부여했습니다. 공개 저장에는 정식 경로·출처·사용권 확인이 필요합니다.
- TOPINFO 전용 1200x630 소셜 카드를 적용해 제품 권리 미확인 합성 이미지를 Open Graph에 사용하지 않습니다.
- Firestore 공개 읽기는 `settings/navigation`, `settings/footer`로 제한하고 `system/**`는 임직원 전용으로 변경했습니다. 로그인 공급자 판정은 클라이언트와 Rules 모두 현재 토큰의 `google.com`을 기준으로 맞췄습니다.
- Firestore Emulator 6/6, 전체 단위 회귀 63/63, TypeScript, 프로덕션 빌드가 통과했습니다. 원격 push, Rules 배포, Hosting 배포는 수행하지 않았습니다.
- 개발 서버가 없는 정적 파일 요청을 `index.html` 200으로 돌려주던 문제를 수정했습니다. 개발·프로덕션 모두 페이지 경로는 200, 정상 자산은 200, 차단 자산은 404로 확인했습니다.
- `탑정보통신 시각 세부 품질 감사` 에셋 검사 결과를 `output/audits/public-assets-gate-2026-07-18.csv`에 남겼습니다. 로컬 서버는 `http://localhost:3000/`에서 실행 중이며 페이지·정상 자산·차단 자산의 HTTP 응답만 확인했습니다. 사용자 지시에 따라 브라우저 자동조작과 현행 화면 시각 재감사는 수행하지 않았습니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 공개 권리 문제는 렌더러 한 곳만 숨겨서는 해결되지 않습니다. 기본 데이터, 기존 Firestore 문서, 관리자 입력, 재생성 스크립트, 물리 파일을 같은 차단 목록으로 묶어야 재발을 막을 수 있습니다.
- 테스트와 빌드를 동시에 실행해 한 번 메모리 부족을 유발했습니다. 상호 독립이어도 리소스가 큰 검증은 순차 실행하는 것이 더 빠르고 확실합니다.
- 자동 테스트 통과는 기술 검증일 뿐 시각 승인과 운영 승인이 아닙니다. 다음 시각 검수는 사용자가 로컬 서버를 연 뒤 변경된 홈·업종·제품 화면만 좁혀 확인하는 방식이 적절합니다.
- 운영 배포 전 남은 P0는 파트너 미디어 재배포 권리와 `settings/security`의 실제 관리자 UID 시딩입니다. App Check는 site key를 넣어 다시 빌드한 뒤 지표를 확인하고 enforcement를 켜야 합니다.

## [60차 작업] 작업 원본 무결성·직원 UID·릴리스 준비 자동 판정 - 2026-07-19

- 상담과 용지 요청을 작업으로 전환할 때 원본과 작업의 상태가 같은 Firestore 트랜잭션에서 함께 바뀌도록 했습니다. 연결된 원본만 단독으로 완료 처리하거나 고객명 등 원본 내용을 임직원 클라이언트에서 바꾸는 요청, 연결 작업을 삭제해 추적을 끊는 요청은 Rules에서 거부합니다.
- 작업 담당자는 표시 이름과 함께 `assigneeId` Google UID를 저장합니다. 직원만 읽을 수 있는 `staff_profiles/{uid}` 디렉터리를 추가해 닉네임 변경 뒤에도 작업 담당자 식별이 유지됩니다.
- CMS 저장 중 초안을 공개 `cms_pages`와 분리한 `cms_page_drafts`에 두고, 직원만 읽고 쓸 수 있도록 Rules와 클라이언트 경로를 맞췄습니다.
- APEXA X와 Toss Front 제품 제원은 제조사·공식 제품 페이지 출처로 정리했습니다. 프로젝트가 소유한 생성·브랜드 자산만 자동 승인하고 POSBANK·Toss Place·AHAPOS·LG U+ 및 업종 기능 화면은 출처가 있어도 로컬 재배포 권리가 확인될 때까지 `pending`으로 판정합니다.
- `npm run release:check`를 추가했습니다. 미디어 권리, 운영 UID, 최신 Rules 배포, 운영 접수 왕복, App Check site key·enforcement, 현행 화면 사용자 승인이 모두 확인되지 않으면 배포 후보 판정을 실패시킵니다.
- 최종 검증은 순차 실행했습니다. `npm test` 65/65, Firestore Emulator 10/10, `npm run lint`, `npm run build`, `git diff --check`가 통과했습니다. 홈·상담·자료실·토스POS 경로는 HTTP 200, 존재하지 않는 정적 파일은 404를 반환했습니다.
- 릴리스 판정은 의도대로 HOLD입니다. 파트너 미디어 권리 35건, 운영 확인 6건, 자료실 manifest 경고 1건이 남아 있습니다. 브라우저 자동조작, 원격 push, Firestore Rules·Hosting 배포는 수행하지 않았습니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 작업관리의 표시 이름만 저장하면 직원 이름 변경이나 동명이인에서 추적이 깨집니다. 불변 UID를 주키로 두고 이름을 표시 정보로만 취급해야 합니다.
- 연결된 두 문서의 상태 일치는 UI 코드만으로 보장할 수 없습니다. 트랜잭션과 `getAfter()` Rules를 함께 사용해야 콘솔 직접 쓰기와 부분 실패까지 막을 수 있습니다.
- 출처 URL이 있다는 사실과 공개 재배포 권리가 있다는 사실은 다릅니다. 앞으로 파트너 자산은 권리 증빙이 등록되기 전 기본 상태를 `pending`으로 유지합니다.
- 자동 테스트는 기능·규칙·빌드 무결성을 보장하지만 현재 화면의 예술적 완성도를 승인하지 않습니다. 사용자가 직접 보는 현행 화면 검수와 제품 원본 권리 확인을 마지막 운영 게이트로 남기는 것이 정확합니다.

## [61차 작업] Toss POS 업종 애니메이션 자동 재생 복구 - 2026-07-19

- 애니메이션 원본 5종을 검사한 결과 WebP/APNG는 12~170프레임이며 모두 무한 반복(`loop=0`)으로 정상입니다. 로컬 서버도 `image/webp`·`image/png`로 원본 바이트를 정상 반환했습니다.
- 재생이 멈춘 원인은 `prefers-reduced-motion: reduce`에서 애니메이션 레이어를 숨기고 정지 fallback을 강제로 표시하는 CSS·렌더 조건이었습니다.
- Toss POS 상세와 홈 업종 장면에서 화면에 들어온 제품 데모 이미지는 항상 원본 애니메이션을 사용하도록 수정했습니다. 동작 줄이기 환경에서는 자동 탭 전환과 CSS 이동 효과는 계속 줄여 접근성 범위를 유지합니다.
- 정지 이미지는 화면 밖 지연 로드와 애니메이션 원본 로드 실패 때만 사용합니다.
- `npm run test:home` 5/5, `npm run lint`가 통과했습니다. 브라우저 자동조작은 수행하지 않았습니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 애니메이션이 멈췄을 때 파일을 다시 변환하기 전에 프레임 수·loop 메타데이터·HTTP Content-Type·CSS 표시 조건 순서로 확인하면 원인을 빠르게 분리할 수 있습니다.
- 정적 fallback은 장애 대응용과 접근성용을 구분해야 합니다. 이번 요구에서는 자동 재생이 핵심 기능이므로 GIF 자체는 유지하고, 자동 장면 전환과 장식 모션만 reduced-motion 대상으로 남겼습니다.

## [62차 작업] Toss POS 카페 보조 이미지 크기 정렬 - 2026-07-19

- 카페·베이커리의 `픽업 주문 연결` 원본은 정사각형에 가까운데 표시 프레임만 `3:2`로 낮게 설정되어, 옆의 `영수증 구성`보다 작아 보였습니다.
- 왼쪽 미디어 프레임을 오른쪽과 같은 `4:3`으로 변경해 두 카드의 외곽 높이를 맞추고 픽업 주문 화면의 시각적 점유율을 키웠습니다.
- 카드 열 너비와 원본 이미지의 `contain` 규칙은 유지해 이미지 잘림이나 왜곡이 생기지 않도록 했습니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 나란히 비교되는 미디어는 원본 비율이 달라도 카드 프레임의 외곽 비율을 먼저 통일해야 시각적 무게가 안정적입니다.
- 제품을 크게 보이게 하려고 `cover`로 자르는 방식보다, 동일 프레임 안에서 `contain`을 유지하고 프레임 높이를 조절하는 방식이 정보 손실을 막습니다.

## [63차 작업] 공개 사이트 타이포 체계·비분리 연락처 정리 - 2026-07-19

- 아이샵케어의 읽기 쉬운 정보 계층, 단어 단위 한글 줄바꿈, 한 줄 CTA 원칙을 참고하되 문구와 화면 구성은 복제하지 않았습니다.
- 공개 CSS에서 같은 역할이 홈·토스POS·일반 페이지·AI전화마다 `64px`, `78px`, `84px` 등으로 달랐던 문제를 `Display`, `Section`, `Subsection`, `Card title`, `Body large`, `Body`, `Small`, `Metadata`, `Control` 역할로 통합했습니다.
- 글자 크기는 뷰포트 너비에 연속 비례시키지 않고 `1440px`, `900px`, `640px` 고정 구간에서만 전환됩니다. 넓은 화면 기준 H1 72px·H2 52px, 일반 데스크톱 64px·48px, 모바일 42px·34px입니다.
- 한글 본문은 `word-break: keep-all`과 자연스러운 문단 줄바꿈을 사용합니다. 전화번호·이메일·사업자등록번호·짧은 CTA는 내부 줄바꿈을 금지했습니다.
- 푸터 회사 정보는 회사명, 대표자, 사업자등록번호, 개인정보 연락처를 각각 독립된 토큰으로 나눠 좁은 화면에서도 항목 사이에서만 줄이 바뀌게 했습니다.
- `탑정보통신 시각 세부 품질 감사`에 역할별 글자 크기 일치와 전화·이메일·식별자 중간 줄바꿈 차단 항목을 추가했습니다.
- `npm run test:typography` 3/3과 `npm run lint`가 통과했습니다. 로컬 서버에서 새 타이포 CSS와 `/toss-pos/`가 HTTP 200을 반환하는 것만 확인했으며, 사용자 지시에 따라 브라우저 자동조작은 수행하지 않았습니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 페이지별 CSS가 계속 덧붙는 구조에서는 개별 숫자를 고치는 방식으로 타이포 편차가 다시 생깁니다. 모든 페이지 전용 CSS 뒤에 공통 계약을 한 번 적용하고 정적 검사로 로드 순서를 고정하는 편이 안정적입니다.
- `word-break: keep-all`만으로 전화번호처럼 하이픈이 있는 토큰의 줄바꿈을 완전히 막을 수 없습니다. 연락처와 식별자는 별도 요소로 감싸 `white-space: nowrap`을 적용해야 합니다.
- 자동 검사는 CSS 계약과 컴파일 무결성만 확인합니다. 실제 화면에서 제목이 어느 지점에서 줄바꿈되는지는 사용자가 홈·토스POS·제품·고객지원의 데스크톱과 모바일 화면을 직접 확인해야 최종 승인할 수 있습니다.

## [64차 작업] TOPINFO 매장 준비 편집형 모듈 - 2026-07-19

- 사용자가 제공한 프로모션·혜택·서비스 모듈 사례에서 큰 제목, 한 패널 한 메시지, 빠른 정보 스캔, 내용과 직접 연결되는 그래픽 원리만 추출했습니다. 경쟁사의 파란색·노란색 광고 구성과 문구는 복제하지 않았습니다.
- 홈의 home-signals 구간을 동일 카드 네 개에서 12열 비대칭 편집 구성으로 바꿨습니다. 신규 오픈, 장비 교체, 통신·보안, 운영 지원이 각각 다른 크기와 의미 그래픽을 사용합니다.
- 그래파이트·오프화이트·라임만 사용해 TOPINFO의 Field Design Desk 분위기에 맞췄습니다. 토스 블루, 보라색 AI 그라데이션, 글래스모피즘, 빈 색상 박스는 사용하지 않았습니다.
- 매장 설계도, 장비 전환, 통신 연결, 설치·AS 창구 그래픽은 Lucide 아이콘과 CSS로 구성했습니다. 제3자 이미지나 가짜 제품, 생성된 한글 UI를 추가하지 않았습니다.
- 기존 CMS의 제목·설명과 이동 목적지는 그대로 유지했습니다. 관리자가 문구를 수정해도 네 상황의 분류와 연결 동작이 유지됩니다.
- 데스크톱은 7:5와 5:7의 비대칭 두 행, 태블릿은 2열, 모바일은 1열로 전환합니다. 전화번호는 한 토큰으로 유지합니다.
- npm run test:home 5/5, npm run lint, git diff --check가 통과했습니다. 사용자 지시에 따라 브라우저 자동조작과 화면 캡처는 수행하지 않았으므로 상태는 구현됨·기술 검증됨이며 시각 승인은 사용자의 로컬 확인이 필요합니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 프로모션 사례의 시각적 강도는 파란색이나 할인 숫자보다 정보 우선순위에서 나옵니다. 공개 조건이 없는 무료·할인·누적 수치를 흉내 내지 않고, 실제 선택 행동을 크게 보여주는 편이 TOPINFO의 신뢰와 전환에 맞습니다.
- 텍스트가 박힌 광고 이미지는 CMS 편집성과 모바일 가독성을 잃습니다. 핵심 문구는 HTML로 유지하고 그래픽만 장식 요소로 분리해야 이후 조건 변경과 접근성 대응이 쉽습니다.
- 다음 시각 확인에서는 홈의 데스크톱과 모바일에서 각 패널의 높이, 제목 줄바꿈, 그래픽 점유율만 좁혀 확인하면 됩니다. 과밀하면 장식을 줄이고, 빈 느낌이면 텍스트를 늘리기보다 그래픽의 스케일과 기준선을 조정하는 것이 적절합니다.
## [65차 작업] 홈 운영 회로·현장 실행표·지원 허브 재설계 - 2026-07-19

- 홈의 `주문 → 결제 → 출력 → 관리` 구간을 얇은 선과 같은 크기의 흰 칸에서 그래파이트 무대 위 고대비 운영 회로로 교체했습니다. 네 단계는 연결 순서를 유지하면서 오프화이트·라임·그래파이트 패널, 큰 기능 아이콘, 명확한 단계 번호로 시각적 우선순위를 갖습니다.
- `설치 전부터 운영 이후까지` 구간은 어두운 상담 데스크와 6단계 현장 실행표로 재구성했습니다. 전화 → 장비 → AS 신호와 각 단계의 실제 아이콘을 추가하고, 통신 설치와 기기 설치 등 중요한 단계가 같은 무게로 묻히지 않도록 패널 성격을 구분했습니다.
- 가맹점 지원 메뉴는 동일 카드 3개에서 하나의 주 업무와 두 개의 보조 업무가 분명한 비대칭 허브로 변경했습니다. CMS의 제목·설명·버튼 문구·이동 경로는 유지했습니다.
- FAQ는 질문 번호, 88px 터치 행, 명확한 열림 상태를 적용했고 최종 상담 CTA는 사이트와 무관한 파란 배너 대신 TOPINFO 그래파이트·라임 체계로 통일했습니다.
- 새 스타일은 `public-home-editorial.css`로 격리하고 기존 공개 타이포 계약보다 먼저 불러오도록 해 글자 역할 체계를 유지했습니다. 보라색 AI 그라데이션, 글래스모피즘, 장식용 원형 오브젝트, 가짜 할인·무료·통계는 추가하지 않았습니다.
- 반응형은 1180px에서 흐름 재배치, 900px에서 2열, 640px에서 1열로 전환하며 전화번호와 CTA는 줄바꿈되지 않습니다.
- `npm run test:home` 5/5, `npm run lint`, `git diff --check`가 통과했습니다. 사용자 지시에 따라 브라우저 자동조작과 화면 캡처는 수행하지 않았으므로 상태는 구현됨·기술 검증됨이며 시각 승인은 사용자의 로컬 확인이 필요합니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 같은 수의 항목이라고 모두 동일 카드 격자로 만들면 주문 흐름, 설치 절차, 지원 업무의 성격 차이가 사라집니다. 앞으로는 순서형 콘텐츠는 회로, 실행 절차는 런북, 도구 모음은 우선순위 허브처럼 정보 구조에 맞는 시각 문법을 먼저 선택합니다.
- 시각적 강도는 텍스트 양이나 가짜 혜택 숫자가 아니라 크기 대비, 색 면적, 아이콘 스케일, 연결 관계에서 만들어야 합니다. 이번 변경은 공개 사실을 늘리지 않고도 스캔 순서를 분명하게 만드는 방향을 사용했습니다.
- 자동 렌더 검사는 CMS 내용과 구조가 살아 있는지 확인하지만 패널 높이와 화면상 리듬을 승인하지는 않습니다. 다음 확인은 사용자가 홈에서 해당 네 구간을 데스크톱과 모바일로 직접 보고, 과밀·과소 구간만 지정하는 방식이 가장 효율적입니다.

## [66차 작업] 헤더 hover 메뉴·원격지원 설치 진입점 - 2026-09-02

- 데스크톱 상단 메뉴 공통 래퍼에 포인터 진입 동작을 연결해 `고객지원`을 포함한 드롭다운이 클릭 없이 열리도록 했습니다. 드롭다운은 같은 래퍼 안에 있어 메뉴로 포인터를 이동하는 동안 닫히지 않습니다.
- 기존 클릭 토글, 키보드 포커스, 바깥 영역 클릭, `Escape` 닫기 동작은 그대로 유지했습니다.
- 상단 `설치·AS 031-487-4401` 전화 링크 바로 왼쪽에 기존 `Monitor` 아이콘을 재사용한 `원격지원 설치` 링크를 추가하고, 요청받은 `https://wonremote-a7fd3.web.app/download/agent`로 직접 연결했습니다.
- 데스크톱 중간 폭에서는 원격지원 링크를 44px 아이콘 버튼으로 축약하고, 모바일 메뉴의 연락처 구역에도 같은 링크를 전체 폭으로 제공했습니다.
- Ponytail 원칙에 따라 새 상태, 타이머, 패키지나 별도 컴포넌트를 추가하지 않고 기존 메뉴 상태와 링크 스타일을 확장했습니다.
- `npm run lint`와 대상 파일 `git diff --check`가 통과했습니다. 사용자 지시에 따라 브라우저 자동조작과 화면 캡처는 수행하지 않았으므로 실제 hover 감각과 헤더 폭은 사용자의 로컬 화면 확인이 필요합니다.

## Retrospective & Future Improvements (회고 및 개선 방향)

- 특정 `고객지원` 메뉴만 예외 처리하면 다른 드롭다운과 조작 방식이 달라집니다. 공통 메뉴 트리거에서 hover를 처리해 탐색 규칙을 일관되게 유지했습니다.
- 현재 메뉴는 포인터가 벗어나도 바깥 영역 클릭, 포커스 이동 또는 `Escape` 전까지 유지됩니다. 향후 자동 닫힘이 필요해지면 드롭다운 진입을 방해하지 않는 지연 닫기 방식과 포인터 경로 검증을 함께 적용해야 합니다.
- 원격지원 링크는 외부 설치 페이지의 운영 상태에 의존합니다. 주소가 변경되면 데스크톱과 모바일 링크를 함께 수정하거나 CMS 관리 항목으로 승격하는 것이 적절합니다.
